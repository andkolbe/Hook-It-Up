import React from 'react';
import Layout from '../components/Layout';

const UserInfo = () => {
    return (
        <Layout>
            <h1 className="text-center">UserInfo
         Page</h1>
        </Layout>
    );
}

export default UserInfo;